/**
 * Legacy entry — community hub now lives in CommunityNavigator (feed tab).
 * Kept so existing imports keep working.
 */
export { CommunityNavigator as CommunitiesScreen } from '../navigation/CommunityNavigator';
